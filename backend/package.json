{
  "name": "proxy-manager-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-port authenticated proxy manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "proxy-chain": "^2.5.6"
  }
}
